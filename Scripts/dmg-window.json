{
  "_comment": "Single source of truth for the styled DMG window geometry. Consumed by make-dmg-background.swift (arrow art), make-dmg-dsstore.py (icon Iloc positions), and verify-dmg-layout.sh (asserts the shipped positions match). Icon x/y use Finder's top-left origin (same as Iloc). Edit here; do not duplicate these numbers in code.",
  "window": { "width": 600, "height": 400 },
  "iconSize": 128,
  "icons": {
    "Rational.app": { "x": 150, "y": 200 },
    "Applications": { "x": 450, "y": 200 }
  }
}
